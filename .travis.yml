language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t arjendevries/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true arjendevries/docker-react npm run test